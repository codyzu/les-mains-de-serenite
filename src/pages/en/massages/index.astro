---
import {getLocalizedSite, site} from '../../../data/site';
import MassagesOverviewPageTemplate from '../../../page-templates/MassagesOverviewPageTemplate.astro';

const locale = 'en' as const;
const localized = getLocalizedSite(locale);
---

<MassagesOverviewPageTemplate
  locale={locale}
  business={site.business}
  localized={localized}
/>
